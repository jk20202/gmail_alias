# CI 用 `npm install`(而非 npm ci)重新解析依赖。
# wrangler 4.x 会把 @cloudflare/workers-types 的 peerOptional 版本不断上调,
# 一旦与 package.json 里锁定的版本不一致,CI 就会 ERESOLVE 直接失败。
# workers-types 仅参与类型检查(peerOptional),这里放宽 peer 解析避免 CI 被上游滚动升级打断。
legacy-peer-deps=true
